# Git-
作为开发者，你是否遇到过这些困扰？ - **项目太多，管理混乱** —— 同时维护几十个 Git 仓库，哪个提交了、哪个没推送，完全记不清 - **重复操作，效率低下** —— 每个项目都要手动 `git add` → `git commit` → `git push`，几十个项目操作下来半天就没了 - **状态不可见** —— 不知道哪些项目有未提交的修改、哪些落后于远程，只能一个个 `git status` 查看 - **Git 报错看不懂** —— 英文错误信息一堆，不知道怎么处理 - **忘记同步** —— 下班前忘了推送代码，第二天换电脑又要折腾
